import { useCallback, useEffect, useMemo, useRef, useState, type WheelEvent } from 'react';
import type { Commit, DiffFile } from '@shared/types';
import { layoutGraph } from '@shared/graph';
import { useStore } from '../store';
import { api, gitCall } from '../lib/api';
import DiffView from '../components/DiffView';
import { Icon } from '../components/Icons';

type Mode = 'lanes' | 'orbit';
type Risk = { files: number; churn: number; score: number; test?: 'pass' | 'fail' };
type Preview = { kind: 'merge' | 'rebase' | 'reset'; ref: string; mode?: 'soft' | 'hard'; summary: string; conflicts?: boolean };
type LaidCommit = ReturnType<typeof layoutGraph>[number];
type OrbitNode = { commit: LaidCommit; x: number; y: number };
const ROW = 62, TOP = 26, LANE = 34;
const COLORS = ['#c4b5fd', '#4fd1c5', '#f687b3', '#63b3ed', '#f6ad55', '#a78bfa'];

export default function GraphWorkspace({ onRebase }: { onRebase?: (branch: string) => void }) {
  const { commits, status, refresh, setToast } = useStore();
  const [mode, setMode] = useState<Mode>(() => localStorage.getItem('luma.graphMode') === 'orbit' ? 'orbit' : 'lanes');
  const [selected, setSelected] = useState<Commit | null>(null);
  const [diff, setDiff] = useState<DiffFile[] | null>(null);
  const [risk, setRisk] = useState<Record<string, Risk>>({});
  const [branchesOpen, setBranchesOpen] = useState(false);
  const [preview, setPreview] = useState<Preview | null>(null);
  const laid = useMemo(() => layoutGraph(commits), [commits]);
  const branches = useMemo(() => {
    const names = new Set<string>();
    for (const commit of laid) for (const value of commit.refs) {
      const name = value.replace('HEAD -> ', '').trim();
      if (name !== 'HEAD' && !name.startsWith('origin/') && !name.startsWith('tag:')) names.add(name);
    }
    return [...names];
  }, [laid]);
  useEffect(() => localStorage.setItem('luma.graphMode', mode), [mode]);
  useEffect(() => { void api.intelInvoke('riskMap').then(result => { if (result.ok && result.data) setRisk(result.data as Record<string, Risk>); }); }, [commits]);
  const choose = useCallback(async (commit: Commit) => {
    setSelected(commit); setDiff(null);
    try { setDiff(await gitCall<DiffFile[]>('commitDiff', commit.hash)); } catch { setDiff([]); }
  }, []);
  useEffect(() => {
    const handle = (event: KeyboardEvent) => {
      if (event.target instanceof HTMLInputElement || event.target instanceof HTMLTextAreaElement || event.target instanceof HTMLSelectElement) return;
      if (event.key === 'Escape') { setSelected(null); setDiff(null); setPreview(null); return; }
      if (event.key !== 'ArrowUp' && event.key !== 'ArrowDown' && event.key !== 'Enter') return;
      const current = Math.max(0, laid.findIndex(commit => commit.hash === selected?.hash));
      if (event.key === 'Enter') { if (laid[current]) void choose(laid[current]); return; }
      event.preventDefault();
      const next = event.key === 'ArrowDown' ? Math.min(laid.length - 1, current + 1) : Math.max(0, current - 1);
      if (laid[next]) void choose(laid[next]);
    };
    window.addEventListener('keydown', handle); return () => window.removeEventListener('keydown', handle);
  }, [choose, laid, selected?.hash]);
  async function action(run: () => Promise<unknown>, message?: string) { try { await run(); await refresh(); if (message) setToast(message); } catch (error) { setToast((error as Error).message); } }
  async function prepare(kind: Preview['kind'], ref: string, resetMode?: 'soft' | 'hard') { try { const result = await api.intelInvoke('preview', kind, ref); if (!result.ok) throw new Error(result.error?.message ?? 'Preview failed'); const data = result.data as { summary?: string; conflicts?: boolean }; setPreview({ kind, ref, mode: resetMode, summary: data.summary || 'No changes reported.', conflicts: data.conflicts }); } catch (error) { setToast((error as Error).message); } }
  async function apply() { if (!preview) return; const operation = preview; setPreview(null); if (operation.kind === 'merge') await action(() => gitCall('merge', operation.ref, false, false), `Merged ${operation.ref}`); else if (operation.kind === 'rebase') await action(() => gitCall('rebase', operation.ref), `Rebased onto ${operation.ref}`); else await action(() => gitCall(operation.mode === 'hard' ? 'rewindHard' : 'rewindSoft', operation.ref), 'Rollback complete'); }
  async function undoRollback() { try { const result = await api.intelInvoke('undoRollback'); if (!result.ok) throw new Error(result.error?.message ?? 'Undo failed'); await refresh(); setToast('Previous rollback restored'); } catch (error) { setToast((error as Error).message); } }
  return <div className="relative flex h-full min-h-0 gap-3"><section className="glass relative flex min-w-0 flex-1 flex-col overflow-hidden"><header className="relative z-20 flex flex-wrap items-center gap-2 border-b border-white/8 px-4 py-2.5"><div><div className="text-[11px] uppercase tracking-wider text-white/50">History</div><div className="text-[9px] text-white/25">Lanes and Orbit share commit details, diffs and rollback actions</div></div><div className="flex overflow-hidden rounded-lg border border-white/10"><button className={`px-2.5 py-1 text-[11px] ${mode === 'lanes' ? 'bg-lilac/20 text-lilac' : 'text-white/50'}`} onClick={() => setMode('lanes')}>Lanes</button><button className={`px-2.5 py-1 text-[11px] ${mode === 'orbit' ? 'bg-lilac/20 text-lilac' : 'text-white/50'}`} onClick={() => setMode('orbit')}>Orbit</button></div><span className="text-[9px] text-white/30">↑ ↓ select · Enter details</span><div className="flex-1"/><button className="btn text-xs text-teal" onClick={() => void undoRollback()}>Undo rollback</button><div className="relative"><button className="btn text-xs" onClick={() => setBranchesOpen(value => !value)}><Icon name="branch"/> Branches</button>{branchesOpen && <BranchMenu branches={branches} current={status?.branch} close={() => setBranchesOpen(false)} action={action} preview={prepare} interactive={onRebase}/>}</div><button className="btn text-xs" onClick={() => void action(() => gitCall('fetch', 'origin'))}>Fetch</button><button className="btn text-xs" onClick={() => void action(() => gitCall('pull'))}>Pull</button><button className="btn btn-primary text-xs" onClick={() => void action(() => gitCall('push', !status?.upstream))}>Push</button></header><div className="min-h-0 flex-1 overflow-auto">{mode === 'lanes' ? <Lanes commits={laid} risk={risk} selected={selected} choose={choose}/> : <Orbit commits={laid} risk={risk} selected={selected} choose={choose}/>}</div></section>{selected && <CommitDetails commit={selected} risk={risk[selected.hash]} diff={diff} close={() => { setSelected(null); setDiff(null); }} prepare={prepare}/>} {preview && <PreviewModal preview={preview} cancel={() => setPreview(null)} apply={apply}/>}</div>;
}
function dotColor(commit: LaidCommit, risk?: Risk) { return risk?.test === 'fail' ? '#fb7185' : risk?.test === 'pass' ? '#4fd1c5' : COLORS[commit.lane % COLORS.length]; }
function radius(base: number, risk?: Risk) { return risk ? Math.min(17, base + risk.score * .07) : base; }
function Lanes({ commits, risk, selected, choose }: { commits: ReturnType<typeof layoutGraph>; risk: Record<string, Risk>; selected: Commit | null; choose: (commit: Commit) => void }) { const rows = new Map(commits.map((commit,index) => [commit.hash,index])), width = 44 + (Math.max(0,...commits.map(commit => commit.lane)) + 1) * LANE, height = Math.max(400, commits.length * ROW + TOP * 2); return <div className="relative min-w-[650px]" style={{ height }}><svg className="absolute inset-0" width={width} height={height}>{commits.flatMap((commit,index) => commit.parents.map(parent => { const parentIndex=rows.get(parent); if(parentIndex===undefined)return null; const target=commits[parentIndex],x=28+commit.lane*LANE,y=TOP+index*ROW+ROW/2,px=28+target.lane*LANE,py=TOP+parentIndex*ROW+ROW/2; return <path key={`${commit.hash}:${parent}`} d={`M${x},${y} C${x},${y+30} ${px},${py-30} ${px},${py}`} fill="none" stroke={COLORS[commit.lane%COLORS.length]} opacity=".5"/>; }))}{commits.map((commit,index) => { const active=selected?.hash===commit.hash,item=risk[commit.hash],x=28+commit.lane*LANE,y=TOP+index*ROW+ROW/2; return <g key={commit.hash}><circle cx={x} cy={y} r={radius(active?10:7,item)} fill={dotColor(commit,item)} stroke={active?'#fff':'transparent'} strokeWidth="3"/></g>; })}</svg>{commits.map((commit,index) => <button key={commit.hash} className={`absolute flex items-center rounded-xl px-3 text-left ${selected?.hash===commit.hash?'bg-lilac/12 ring-1 ring-lilac/35':'hover:bg-white/5'}`} style={{top:TOP+index*ROW,left:width,right:8,height:ROW-6}} onClick={() => void choose(commit)}><span className="min-w-0 flex-1"><span className="block truncate text-[13px] text-white/85">{commit.message}</span><span className="block text-[11px] text-white/45">{commit.shortHash} · {commit.author}</span></span>{commit.refs.length>0&&<span className="ml-3 max-w-48 truncate rounded-full border border-teal/25 bg-teal/8 px-2 py-0.5 text-[9px] text-teal">{commit.refs.join(' · ')}</span>}</button>)}</div>; }
function Orbit({ commits, risk, selected, choose }: { commits: ReturnType<typeof layoutGraph>; risk: Record<string, Risk>; selected: Commit | null; choose: (commit: Commit) => void }) { const holder=useRef<HTMLDivElement>(null),drag=useRef<{x:number;y:number;clientX:number;clientY:number;scale:number}|null>(null),moved=useRef(false); const [size,setSize]=useState({width:800,height:600}),[camera,setCamera]=useState({x:0,y:0,scale:1}),[hovered,setHovered]=useState<LaidCommit|null>(null); useEffect(()=>{const element=holder.current;if(!element)return;const observer=new ResizeObserver(()=>setSize({width:element.clientWidth,height:element.clientHeight}));observer.observe(element);return()=>observer.disconnect()},[]); const nodes=useMemo<OrbitNode[]>(()=>commits.slice(0,400).map((commit,index,source)=>{if(index===0)return{commit,x:size.width/2,y:size.height/2};const progress=source.length>1?index/(source.length-1):0,angle=index*2.39996,r=58+progress*Math.min(size.width,size.height)*.4;return{commit,x:size.width/2+Math.cos(angle)*r,y:size.height/2+Math.sin(angle)*r}}),[commits,size]),byHash=useMemo(()=>new Map(nodes.map(node=>[node.commit.hash,node])),[nodes]); const zoom=useCallback((scale:number)=>setCamera(current=>({...current,scale:Math.max(.35,Math.min(4,scale))})),[]); const wheel=(event:WheelEvent<SVGSVGElement>)=>{event.preventDefault();zoom(camera.scale*Math.exp(-event.deltaY*.0015))}; return <div ref={holder} className="relative h-full min-h-[440px] overflow-hidden"><div className="absolute right-3 top-3 z-10 rounded-xl border border-white/10 bg-black/45 p-1"><button className="px-2" onClick={()=>zoom(camera.scale/1.25)}>−</button><span className="px-2 text-[10px]">{Math.round(camera.scale*100)}%</span><button className="px-2" onClick={()=>zoom(camera.scale*1.25)}>+</button><button className="px-2 text-[10px]" onClick={()=>setCamera({x:0,y:0,scale:1})}>Fit</button></div><svg width={size.width} height={size.height} viewBox={`${camera.x} ${camera.y} ${size.width/camera.scale} ${size.height/camera.scale}`} className="absolute inset-0 touch-none cursor-grab active:cursor-grabbing" onWheel={wheel} onPointerDown={event=>{if((event.target as Element).closest('[data-orbit-node]'))return;event.currentTarget.setPointerCapture(event.pointerId);moved.current=false;drag.current={...camera,clientX:event.clientX,clientY:event.clientY}}} onPointerMove={event=>{if(!drag.current)return;const start=drag.current,dx=event.clientX-start.clientX,dy=event.clientY-start.clientY;if(Math.abs(dx)+Math.abs(dy)>4)moved.current=true;setCamera({x:start.x-dx/start.scale,y:start.y-dy/start.scale,scale:start.scale})}} onPointerUp={event=>{if(drag.current)event.currentTarget.releasePointerCapture(event.pointerId);drag.current=null}}>{nodes.flatMap(node=>node.commit.parents.map(parent=>{const target=byHash.get(parent);return target?<line key={`${node.commit.hash}:${parent}`} x1={node.x} y1={node.y} x2={target.x} y2={target.y} stroke={COLORS[node.commit.lane%COLORS.length]} opacity=".35"/>:null}))}{nodes.map(node=>{const item=risk[node.commit.hash],active=selected?.hash===node.commit.hash,nodeRadius=radius(active?12:7,item);return <g data-orbit-node="true" key={node.commit.hash} className="cursor-pointer" onPointerEnter={()=>setHovered(node.commit)} onPointerLeave={()=>setHovered(null)} onClick={()=>{if(!moved.current)void choose(node.commit)}}><circle cx={node.x} cy={node.y} r={active?nodeRadius+9:nodeRadius+3} fill={active?'rgba(139,92,246,.25)':'transparent'} stroke={active?'rgba(196,181,253,.8)':'transparent'} strokeWidth="2"/><circle cx={node.x} cy={node.y} r={nodeRadius} fill={dotColor(node.commit,item)} stroke={active?'#fff':'transparent'} strokeWidth="3"/><text x={node.x} y={node.y+nodeRadius+15} textAnchor="middle" fontSize={active?'11':'9'} fill={active?'rgba(255,255,255,.92)':'rgba(255,255,255,.6)'}>{node.commit.shortHash}</text>{node.commit.refs.length>0&&<text x={node.x} y={node.y-nodeRadius-9} textAnchor="middle" fontSize="8" fill="#4fd1c5">{node.commit.refs[0].slice(0,24)}</text>}</g>})}</svg>{hovered&&selected?.hash!==hovered.hash&&<div className="pointer-events-none absolute bottom-4 left-4 max-w-sm rounded-xl border border-lilac/25 bg-[#0b1020]/95 p-3 shadow-2xl"><div className="truncate text-xs text-white/85">{hovered.message}</div><div className="mt-1 font-mono text-[10px] text-lilac">{hovered.shortHash}</div><div className="mt-1 text-[10px] text-white/40">{hovered.author} · {new Date(hovered.timestamp*1000).toLocaleString()}</div><div className="mt-2 text-[9px] text-white/25">Click for diff and the same actions as Lanes</div></div>}</div>; }
function CommitDetails({commit,risk,diff,close,prepare}:{commit:Commit;risk?:Risk;diff:DiffFile[]|null;close:()=>void;prepare:(kind:'merge'|'rebase'|'reset',ref:string,mode?:'soft'|'hard')=>Promise<void>}){return <aside aria-label="Commit details" className="glass relative flex w-[38%] min-w-[330px] flex-col overflow-hidden"><button aria-label="Close commit details" title="Close (Esc)" className="absolute right-3 top-3 z-20 flex h-8 w-8 items-center justify-center rounded-lg border border-white/10 bg-black/20 text-lg text-white/60 hover:border-rose/50 hover:text-rose" onClick={close}>×</button><div className="border-b border-white/10 px-4 py-3 pr-14"><div className="text-sm">{commit.message}</div><div className="mt-1 text-[11px] text-white/45">{commit.author} · {commit.hash.slice(0,10)}</div><div className="mt-1 text-[10px] text-white/30">{new Date(commit.timestamp*1000).toLocaleString()}</div>{commit.refs.length>0&&<div className="mt-2 flex flex-wrap gap-1">{commit.refs.map(ref=><span key={ref} className="rounded border border-teal/25 px-1.5 py-0.5 text-[9px] text-teal">{ref}</span>)}</div>}{risk&&<div className="mt-2 rounded-lg border border-white/10 p-2 text-[10px] text-white/55">Risk {risk.score}/100 · {risk.files} files · {risk.churn} changed lines · local test {risk.test??'not run'}</div>}<div className="mt-3 flex flex-wrap gap-2"><button className="btn text-[10px] text-amber" onClick={()=>void prepare('reset',commit.hash,'soft')}>Preview soft rollback</button><button className="btn btn-danger text-[10px]" onClick={()=>void prepare('reset',commit.hash,'hard')}>Preview hard rollback</button></div></div><div className="min-h-0 flex-1 overflow-y-auto">{diff===null&&<div className="p-4 text-xs">Loading diff…</div>}{diff?.length===0&&<div className="p-4 text-xs text-white/40">No displayable file diff.</div>}{diff?.map(file=><DiffView key={`${file.oldPath}:${file.newPath}`} file={file}/>)}</div></aside>}
function BranchMenu({branches,current,close,action,preview,interactive}:{branches:string[];current?:string;close:()=>void;action:(run:()=>Promise<unknown>,message?:string)=>Promise<void>;preview:(kind:'merge'|'rebase',ref:string)=>Promise<void>;interactive?:((branch:string)=>void)}){const[name,setName]=useState('');return <div className="glass absolute right-0 top-10 z-30 w-72 p-2"><input className="field mb-2 w-full text-xs" placeholder="New branch…" value={name} onChange={event=>setName(event.target.value)}/><button className="btn btn-primary mb-2 w-full text-xs" disabled={!name.trim()} onClick={()=>{close();void action(()=>gitCall('checkout','HEAD',name.trim()))}}>Create and switch</button>{branches.map(branch=><div key={branch} className="flex items-center gap-2 rounded-lg px-2 py-1.5 hover:bg-white/8"><button className={`min-w-0 flex-1 truncate text-left text-xs ${branch===current?'text-teal':''}`} onClick={()=>{close();if(branch!==current)void action(()=>gitCall('checkout',branch))}}>{branch}</button>{branch!==current&&<><button className="text-[9px] text-lilac" onClick={()=>{close();void preview('merge',branch)}}>merge</button><button className="text-[9px] text-amber" onClick={()=>{close();void preview('rebase',branch)}}>rebase</button>{interactive&&<button className="text-[9px]" onClick={()=>{close();interactive(branch)}}>edit</button>}</>}</div>)}</div>}
function PreviewModal({preview,cancel,apply}:{preview:Preview;cancel:()=>void;apply:()=>Promise<void>}){return <div className="absolute inset-0 z-50 flex items-center justify-center bg-black/65 p-8"><div className="glass flex max-h-[80vh] w-full max-w-3xl flex-col p-5"><div className="flex items-center gap-3"><div><div className="text-sm font-semibold">{preview.kind.toUpperCase()} preview · {preview.ref}</div><div className={preview.conflicts?'text-xs text-rose':'text-xs text-teal'}>{preview.conflicts?'Possible conflicts detected':'Read-only preview complete'}</div></div><div className="flex-1"/><button className="btn" onClick={cancel}>Cancel</button><button className={`btn ${preview.mode==='hard'||preview.conflicts?'btn-danger':'btn-primary'}`} onClick={()=>void apply()}>Apply {preview.mode??preview.kind}</button></div><pre className="mt-4 overflow-auto whitespace-pre-wrap rounded-xl bg-black/30 p-4 text-[11px] text-white/65">{preview.summary}</pre></div></div>}
